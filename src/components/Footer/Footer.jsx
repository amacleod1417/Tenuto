import React from 'react';
import { View, Text, Image, TouchableOpacity, Alert } from 'react-native';
import styles from '../../../styles';

const Footer = () => {
  return (
    <Text style={styles.smallLogo}>tenuto</Text>
  );
};

export default Footer;
